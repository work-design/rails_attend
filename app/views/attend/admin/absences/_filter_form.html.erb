<%= form_with theme: 'search', model: Attend::Absence.new do |f| %>
  <div class="field-body">
    <%= f.text_field 'member.name' %>
    <div class="field is-narrow">
      <%= f.submit %>
    </div>
  </div>
<% end %>
