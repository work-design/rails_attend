<div class="ui modal" id="modal">
  <i class="close icon"></i>
  <div class="ui header blue">
    Add Note
  </div>

  <div class="content">
    <%= form_with model: @attendance_log, url: admin_attendance_logs_path do |f| %>
      <%= render partial: 'form', locals: { f: f } %>
    <% end %>
  </div>

</div>
