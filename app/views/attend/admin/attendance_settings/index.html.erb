<%= render layout: 'index_table', locals: { cache_key: Attend::AttendanceSetting.column_names.hash } do %>
  <%= render partial: 'index_tbody', collection: @attendance_settings, as: :model %>
<% end %>

<%= paginate @attendance_settings %>
