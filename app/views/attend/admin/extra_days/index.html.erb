<%= render 'financial_month_menu' %>

<%= render layout: 'index_table', locals: { cache_key: Attend::ExtraDay.column_names.hash } do %>
  <%= render partial: 'index_tbody', collection: @extra_days, as: :model %>
<% end %>

<%= paginate @extra_days %>
