<div class="ui segment breadcrumb">
  <%= link_to 'Back', my_overtimes_path, class: 'section' %>
  <div class="divider"> / </div>
  <div class="active section">Edit</div>
</div>

<div class="ui segment">
  <%= form_with model: @overtime, scope: :overtime, url: my_overtime_path(@overtime), local: true do |f| %>
    <%= render partial: 'form', locals: {f: f} %>
  <% end %>
</div>
