<%= render 'menu' %>

<%= render layout: 'index_table', locals: { cache_key: Attend::Overtime.column_names.hash } do %>
  <%= render partial: 'index_tbody', layout: 'index_tr', collection: @overtimes, as: :model %>
<% end %>

<%= paginate @overtimes %>
